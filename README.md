# Impressionador ULTRA 3D++ (Windows) 🚀

✅ Você pediu e foi feito:
- **Cena 9 (Tunnel)** agora tem **motion blur fake** (persistência temporal / trilhas)
- **Cena 11 (Terrain)** agora tem **shading por normal** (luz/sombra no relevo)

## 🎮 Controles
- Troca rápida: `1..9`
- Durante execução: `J` = cena **10** (Cubo), `K` = cena **11** (Terreno)
- `T` tema | `C` cor | `+/-` velocidade | `P` pausa | `R` reset | `H` ajuda | `Q/ESC` menu

## ▶️ Rodando
Duplo clique em `run.bat`.

Manual:
```powershell
chcp 65001
python impressionador_ultra_3dpp.py
```

## 🖥️ Mais detalhe = mais colunas
```bat
mode con: cols=200 lines=60
```

## Dicas rápidas
- Quer trilhas mais evidentes no túnel? Aumente a velocidade com `+`.
- Quer ver bem o sombreamento do terreno? Use o tema **Normal** ou **Suave**.
